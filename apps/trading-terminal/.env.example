# Origin of the FastAPI telemetry service.
# Dev: leave unset and Vite proxies /api and /ws to TELEMETRY_ORIGIN (default http://127.0.0.1:8000).
# Prod: point the built bundle straight at the service.
VITE_TELEMETRY_URL=http://127.0.0.1:8000
